# CLAUDE.md

本文件提供跨项目的默认软件工程原则, 按当前任务和项目的明确约束调整.

目标: 通过短反馈循环交付有价值且可验证的行为, 持续降低软件的理解与修改成本.

## 敏捷开发

- 以解决用户问题的可用成果衡量进展. 优先交付小而完整, 可以验证的行为, 尽早获取真实反馈; 需求越不确定, 反馈循环越短.
- 保持与用户协作, 通过反馈澄清需求, 及时调整验收条件和计划. 响应需求变化, 不固守已经失效的方案; 流程和工具应服务于交付与协作.
- 设计贯穿每次变更. 在实现, 验证和反馈的循环中修正设计, 按需重构并再次验证. 持续改善代码与测试, 保持后续变更容易理解, 实施和验证.
- 修改前阅读相关代码, 调用方, 测试和配置, 理解现有模型与约定. 识别工作区已有变更, 不覆盖或撤销与当前任务无关的工作.
- 明确需求, 验收条件和必须保持的行为. 优先从项目中消除疑问; 对局部, 可逆的实现选择自行判断. 无法从现有信息判断且会影响需求含义, 外部契约或不可逆结果的歧义, 先澄清.
- 只做足够支持当前需求的设计, 不为假想需求预建扩展点, 配置或通用框架. 推迟可以安全推迟的决定; 对影响广泛或难以逆转的变更, 先验证关键假设和恢复方案.

## 代码与状态

- 以代码和行为测试作为主要规范与文档, 用命名, 类型和结构表达领域概念与契约. 实现反映当前行为, 需求与验收决定行为是否正确.
- 命名准确, 与上下文一致, 避免含义不明的泛称. 名称需要额外解释时, 先考虑改名; 结构难以说明时, 先检查职责划分.
- 注释说明代码难以表达的原因, 约束和外部异常, 不复述实现或掩盖结构问题. 额外文档按需维护, 不重复可从代码和配置可靠获得的信息; 修改行为时同步修正受影响的说明.
- 明确依赖, 数据流, 状态归属和副作用. 优先局部状态和直接控制流, 将外部副作用放在明确边界, 减少共享可变状态, 隐式初始化顺序和不透明的间接调用.
- 同一事实有明确的权威来源. 派生信息优先计算或生成; 必须保存副本时, 明确同步与失效规则, 避免多处手工维护同一事实.

## 模块与接口

- 函数承担清晰职责, 模块围绕内聚的领域能力组织. 协调完整流程可以是一项职责, 但协调逻辑不混入各步骤的实现细节. 按职责和变化原因划分边界, 不按行数机械拆分.
- 目录和文件组织应体现职责与模块边界, 让人和 Agent 能通过命名与层级理解仓库结构, 定位入口并找到修改位置. 将紧密相关的实现放在一起, 避免无关职责混放和过度拆分; 遵循项目惯例, 不为形式统一增加层级.
- 用简单接口隐藏复杂实现和易变决策, 让相关知识与变化留在同一边界内. 模块的价值在于替调用者省去多少知识; 不把内部步骤和中间状态的管理推给调用者, 也不合并无关职责.
- 只公开当前调用者需要的能力, 保持内部实现可替换. 让常见用法简单, 错误用法困难; 参数在调用处含义明确, 避免含义不明的布尔开关.
- 同一接口的不同实现遵守共同的行为契约. 替换实现不能擅自收紧合法输入, 削弱结果保证或改变约定的失败语义. 修改契约前检查实际调用和兼容要求.

## 简化与演进

- 精简前明确必须保留的最小完整流程及其成功与失败行为, 据此界定删改范围和验证方式. 优先通过删除, 替换和简化现有实现解决问题, 避免持续叠加分支, 包装和并行路径.
- 合并同一领域概念的重复表达, 收敛无实际需求支撑的配置项, 运行模式和隐式回退, 减少需要理解和验证的状态组合与行为路径. 不以文件数或代码行数减少代替简化效果.
- 替代实现完成后, 清理失去用途的旧代码, 配置, 依赖和接口. 删除前确认实际使用与兼容要求; 用途不明时先查证, 不把不了解用途当作无用的证据. 必须保留的过渡方案应有明确的退出条件.
- 抽象应降低总体理解与修改成本. 允许暂时重复, 根据共同语义和实际变化提炼抽象, 不因代码相似或套用模式增加层次. 特例和跨模块联动持续增加时, 重新检查模型与边界.
- 优先复用项目已有能力. 引入依赖前确认实际收益与兼容性, 计入接入, 升级和维护成本; 不因自有代码变少就忽略转移到依赖或配置中的复杂性.
- 修正根因, 同时保持或改善相关结构, 按需进行有限重构. 不为缩小差异保留错误设计, 不夹带无关重写, 不以压缩表达或删除必要验证换取行数下降.

## 正确性与失败处理

- 在系统边界校验外部输入, 用类型和数据模型表达约束, 减少非法状态. 不在内部反复防御已经成立的契约, 不依赖未声明的实现细节维持正确性.
- 错误在能够正确处理的层处理, 否则携带必要上下文向上传递. 保留调用者判断失败原因所需的事实; 不吞错, 伪装成功或用无依据的默认值掩盖失败, 不在错误信息中泄露敏感数据.
- 明确操作失败后的状态和资源释放责任. 涉及持久化, 并发或外部调用时, 按实际需求处理一致性, 部分失败与恢复; 重试应有界, 并避免重复副作用.

## 验证与交付

- 对已确定且能可靠自动验证的约束, 优先通过类型, 测试或静态检查落实, 复用项目已有检查机制. 硬性门槛来自实际契约和项目要求, 不凭空规定通用阈值.
- 验证依据来自需求, 外部行为和模块契约, 覆盖与风险相称的关键路径, 边界和失败情况. 修复缺陷时, 优先建立能复现问题的回归验证. 检查范围应覆盖实际变更影响, 包括必要的集成行为.
- 测试应允许内部实现自由重构, 避免绑定私有函数, 无关调用次数或中间结构. 不复述实现来计算预期, 不为测试数量或覆盖率堆砌测试; 难以测试时, 先检查职责, 依赖与副作用.
- 检查失败时先定位原因, 区分已有问题和本次引入的问题. 不通过跳过检查, 削弱断言或无依据地更新预期掩盖失败; 需求或契约确实改变时, 同步修正验证依据.
- 将代码量, 分支, 依赖和接口数量的增长作为审查信号, 不当作质量结论. 性能和资源优化依据实际约束, 用代表性数据测量和验证, 不为猜测增加复杂机制.
- 完成前审阅实际差异, 运行相关检查并核对验收条件. 交付时说明结果, 实际验证与剩余限制, 不把未运行或局部检查说成全部通过. 需求已满足且验证充分后结束当前变更, 不为继续优化扩大范围.

## 权衡原则

以正确性和明确约束为底线, 优先清晰性与可修改性, 在此基础上减少需要维护的代码和概念. 成本包含理解, 审查, 验证, 运行, 迁移和回滚, 不能只计算编码时间. 依据实际证据权衡, 不机械套用原则或追逐单一指标.

每次修改都应回答: 当前需求是否得到验证, 下一次相关修改是否仍然容易?
